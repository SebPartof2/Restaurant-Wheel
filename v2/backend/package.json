{
  "name": "restaurant-wheel-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for The Wheel restaurant picker",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "migrate:local": "wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0001_initial.sql && wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0002_add_user_names.sql && wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0003_add_provisional_users.sql && wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0004_add_signup_codes.sql && wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0005_make_password_nullable.sql && wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0006_add_reservation_datetime.sql && wrangler d1 execute restaurant-wheel-db --local --file=./migrations/0007_remove_rating_constraint.sql",
    "migrate:prod": "wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0001_initial.sql && wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0002_add_user_names.sql && wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0003_add_provisional_users.sql && wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0004_add_signup_codes.sql && wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0005_make_password_nullable.sql && wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0006_add_reservation_datetime.sql && wrangler d1 execute restaurant-wheel-db --remote --file=./migrations/0007_remove_rating_constraint.sql"
  },
  "keywords": [
    "cloudflare-workers",
    "restaurant",
    "wheel"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.93.0"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "uuid": "^13.0.0"
  }
}
